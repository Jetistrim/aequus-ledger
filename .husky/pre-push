echo "► [pre-push] Validando versao de release..."
node scripts/check-release-version.mjs

echo "► [pre-push] Build completo do backend..."
npm run build --prefix backend

echo "► [pre-push] Build completo do frontend..."
npm run build --prefix frontend
